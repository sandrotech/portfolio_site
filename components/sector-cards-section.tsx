import { LeadCta } from "@/components/lead-cta"
import { sectorIds, sectorMeta } from "@/lib/sectors"

export function SectorCardsSection() {
  return <section id="sectors" className="scroll-mt-24 py-20 sm:py-24"><div className="container mx-auto px-4"><div className="mx-auto max-w-7xl"><div className="text-center"><p className="section-eyebrow">Possibilidades por segmento</p><h2 className="section-title">Tecnologia aplicada a diferentes operações</h2><p className="section-copy mx-auto">A STech desenvolve sistemas, integrações e automações adaptados aos processos de cada empresa. Os exemplos abaixo mostram algumas possibilidades de aplicação, sem limitar a atuação a esses segmentos.</p></div><div className="mt-10 grid gap-5 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4">{sectorIds.map((sectorId) => { const sector = sectorMeta[sectorId]; const Icon = sector.icon; return <article key={sectorId} className="flex h-full flex-col rounded-2xl border border-border bg-card p-6 shadow-[0_8px_24px_rgba(49,91,138,0.07)]"><div className="flex h-11 w-11 items-center justify-center rounded-xl border border-accent/20 bg-card-highlight"><Icon className="h-6 w-6 text-accent" aria-hidden="true" /></div><h3 className="mt-5 text-xl font-bold">{sector.label}</h3><p className="mt-3 flex-1 leading-relaxed text-muted-foreground">{sector.description}</p><LeadCta source="sector" sector={sectorId} event="sector_cta_click" label="Conversar sobre este segmento" initialMessage={`Gostaria de conversar sobre uma solução para ${sector.label.toLowerCase()}.`} className="mt-6 w-full" /></article> })}</div></div></div></section>
}
